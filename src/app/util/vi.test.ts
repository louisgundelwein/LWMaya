import { describe, expect, it } from 'vitest';
import { isOdd } from './vi';



describe('isOdd', () => {
	it('isOdd checks that a number is odd', () => {
    const   = isOdd(3);
    expect(result).toEqual(false);
  });
});
